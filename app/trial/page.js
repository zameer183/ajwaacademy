'use client'; import { useEffect, useState } from 'react'; import Link from 'next/link'; import { trialAPI } from '@/lib/static-api'; export default function TrialAccessPage() { const [trials, setTrials] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { const loadTrials = async () => { setLoading(true); const data = await trialAPI.getUserTrials(); setTrials(Array.isArray(data) ? data : []); setLoading(false); }; loadTrials(); }, []); return ( <div className="min-h-screen bg-gray-50 py-10"> <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8"> <h1 className="text-3xl font-bold text-gray-900 mb-6">Trial Access</h1> {loading ? ( <div className="text-gray-600">Loading...</div> ) : trials.length === 0 ? ( <div className="text-gray-600">No active trials yet.</div> ) : ( <div className="grid grid-cols-1 md:grid-cols-2 gap-6"> {trials.map((trial) => ( <div key={trial.id} className="bg-white rounded-lg shadow-md p-6"> <h2 className="text-lg font-semibold text-gray-900"> {trial.course_title || 'Trial Course'} </h2> <p className="text-sm text-gray-600 mt-1"> Expires: {trial.expires_at ? new Date(trial.expires_at).toLocaleString() : 'N/A'} </p> <Link href={`/dashboard/course/${trial.course_id}`} className="inline-block mt-4 bg-[rgba(0,0,102)] text-white px-4 py-2 rounded-md font-semibold hover:bg-[rgba(51,102,153)] transition-colors" > Open Trial </Link> </div> ))} </div> )} </div> </div> ); } 

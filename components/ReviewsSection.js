'use client'; import { useEffect, useState } from 'react'; import Image from 'next/image'; import { reviewAPI } from '@/lib/static-api'; export default function ReviewsSection({ title = 'Student Reviews' }) { const [reviews, setReviews] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { const loadReviews = async () => { try { const data = await reviewAPI.getReviews(); setReviews(Array.isArray(data) ? data : []); } catch (error) { console.error('Failed to load reviews:', error); setReviews([]); } finally { setLoading(false); } }; loadReviews(); }, []); return ( <section className="py-16 bg-gray-50"> <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"> <div className="text-center mb-10"> <h2 className="text-3xl font-bold text-gray-900">{title}</h2> <div className="w-20 h-1 bg-[rgba(0,0,102)] rounded-full mt-4 mx-auto"></div> <p className="text-gray-600 mt-2">What our learners say about Ajwa Academy</p> </div> {loading ? ( <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"> {Array.from({ length: 6 }).map((_, index) => ( <div key={index} className="bg-white rounded-lg p-6 shadow-md"> <div className="h-4 bg-gray-200 rounded w-2/3 mb-3 animate-pulse"></div> <div className="h-3 bg-gray-200 rounded w-full mb-2 animate-pulse"></div> <div className="h-3 bg-gray-200 rounded w-3/4 animate-pulse"></div> </div> ))} </div> ) : reviews.length === 0 ? ( <div className="text-center text-gray-600">No reviews yet.</div> ) : ( <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"> {reviews.map((review) => ( <div key={review.id} className="bg-white rounded-lg p-6 shadow-md"> <div className="flex items-center mb-4"> <div className="w-12 h-12 rounded-full overflow-hidden mr-3 bg-gray-100"> {review.image ? ( <Image src={review.image} alt={review.author || 'Reviewer'} width={48} height={48} className="object-contain w-full h-full" unoptimized loading="lazy" /> ) : ( <div className="w-full h-full bg-gray-200"></div> )} </div> <div> <h3 className="text-sm font-semibold text-gray-900">{review.author || 'Student'}</h3> <p className="text-xs text-gray-500">{review.title || 'Review'}</p> </div> </div> <div className="text-sm text-gray-700 mb-3"> {review.comment || 'Great learning experience with supportive teachers.'} </div> <div className="text-xs text-gray-500">Rating: {review.rating || 5}/5</div> {review.video && ( <a href={review.video} target="_blank" rel="noreferrer" className="mt-3 inline-block text-xs text-[rgba(0,0,102)] hover:underline" > View Video </a> )} </div> ))} </div> )} </div> </section> ); } 

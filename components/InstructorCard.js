import Image from 'next/image'; export default function InstructorCard({ instructor, avatar, bio, coursesCount, studentsCount, rating, reviews }) { const hasRating = typeof rating === 'number' && !Number.isNaN(rating); const hasCourses = typeof coursesCount === 'number' && !Number.isNaN(coursesCount); const hasStudents = typeof studentsCount === 'number' && !Number.isNaN(studentsCount); const showStats = hasCourses || hasStudents; return ( <div className="bg-white rounded-lg shadow-md p-6"> <div className="flex items-center mb-4"> {avatar ? ( <div className="w-16 h-16 rounded-full overflow-hidden mr-4"> <Image src={avatar} alt={instructor || 'Instructor'} width={64} height={64} className="object-cover" /> </div> ) : null} <div> <h3 className="text-lg font-bold text-gray-900">{instructor || 'Instructor'}</h3> {hasRating && ( <div className="flex items-center mt-1"> <div className="flex text-yellow-400"> {[...Array(5)].map((_, i) => ( <svg key={i} className={`w-4 h-4 ${i < Math.floor(rating) ? 'fill-current' : 'text-gray-300'}`} viewBox="0 0 24 24" > <path d="M12 17.27L18.18 21l-1.64-7.03L22 9.24l-7.19-.61L12 2 9.19 8.63 2 9.24l5.46 4.73L5.82 21z" /> </svg> ))} </div> <span className="text-sm text-gray-600 ml-1"> {rating} {typeof reviews === 'number' ? `(${reviews})` : ''} </span> </div> )} </div> </div> {bio ? ( <p className="text-gray-600 text-sm mb-4"> {bio} </p> ) : null} {showStats ? ( <div className="grid grid-cols-2 gap-4 text-center"> <div> <div className="text-2xl font-bold text-[rgba(0,0,102)]"> {hasCourses ? coursesCount : '—'} </div> <div className="text-xs text-gray-500">Courses</div> </div> <div> <div className="text-2xl font-bold text-[rgba(0,0,102)]"> {hasStudents ? studentsCount.toLocaleString() : '—'} </div> <div className="text-xs text-gray-500">Students</div> </div> </div> ) : null} </div> ); } 

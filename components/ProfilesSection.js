'use client'; import { useEffect, useState } from 'react'; import Image from 'next/image'; import { profileAPI } from '@/lib/static-api'; export default function ProfilesSection({ title = 'Our Team' }) { const [profiles, setProfiles] = useState([]); const [loading, setLoading] = useState(true); useEffect(() => { const loadProfiles = async () => { try { const data = await profileAPI.getProfiles(); setProfiles(Array.isArray(data) ? data : []); } catch (error) { console.error('Failed to load profiles:', error); setProfiles([]); } finally { setLoading(false); } }; loadProfiles(); }, []); return ( <section className="py-16"> <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8"> <div className="text-center mb-10"> <h2 className="text-3xl font-bold text-gray-900">{title}</h2> <div className="w-20 h-1 bg-[rgba(0,0,102)] rounded-full mt-4 mx-auto"></div> <p className="text-gray-600 mt-2">Meet the people behind Ajwa Academy</p> </div> {loading ? ( <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6"> {Array.from({ length: 4 }).map((_, index) => ( <div key={index} className="bg-white rounded-lg p-6 shadow-md"> <div className="h-16 w-16 rounded-full bg-gray-200 mb-4 animate-pulse"></div> <div className="h-4 bg-gray-200 rounded w-2/3 mb-2 animate-pulse"></div> <div className="h-3 bg-gray-200 rounded w-1/2 animate-pulse"></div> </div> ))} </div> ) : profiles.length === 0 ? ( <div className="text-center text-gray-600">No profiles yet.</div> ) : ( <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6"> {profiles.map((profile) => ( <div key={profile.id} className="bg-white rounded-lg p-6 shadow-md text-center"> <div className="w-20 h-20 rounded-full overflow-hidden mx-auto mb-4 bg-gray-100"> <Image src={profile.avatar || '/teacher-man.jpeg'} alt={profile.name || 'Profile'} width={80} height={80} className="object-contain w-full h-full" unoptimized loading="lazy" /> </div> <h3 className="text-lg font-semibold text-gray-900">{profile.name || 'Team Member'}</h3> <p className="text-sm text-gray-600">{profile.role || profile.email}</p> </div> ))} </div> )} </div> </section> ); } 
